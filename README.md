# mars-irt

## TO-DO
- finalize sampling approach: totally random parameters or semi-random parameters?
- run models on cluster + make analysis plots, look at general range of items selected
- select final items to run
- decide on sampling strategy for nivturk